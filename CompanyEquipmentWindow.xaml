<Window x:Class="ntra_missions.CompanyEquipmentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ntra_missions"
        mc:Ignorable="d"
        Title="CompanyEquipmentWindow" Height="600" Width="800"  ResizeMode="NoResize" Background="#F2F4F9">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition/>
            <RowDefinition Height="75"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="#000080">
            <Image Source="Photos\NTRA_white1.png" VerticalAlignment="Center" Height="70"  HorizontalAlignment="Left" Margin="16,0,0,0"/>
            <Label Name="headerLabel" Content="Equipment" Style="{StaticResource headerLabelStyle}" HorizontalAlignment="Center"/>
        </Grid>

        <ScrollViewer Grid.Row="1" Margin="48">
            <Grid Name="mainGrid">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>


                <WrapPanel Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <Label Content="Unit Name: " Style="{StaticResource mediumLabelStyle}" />
                    <TextBox Name="unitNameTextBox" Style="{StaticResource textboxStyle}"/>
                </WrapPanel>
                <WrapPanel Grid.Row="1" Grid.ColumnSpan="2" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <Label Content="Unit Type: " Style="{StaticResource mediumLabelStyle}"/>
                    <ComboBox Name="unitTypeComboBox" Style="{StaticResource comboBoxStyle}" />
                </WrapPanel>

                
            </Grid>
        </ScrollViewer>

        <Button Grid.Row="2" Name="finishButton" Content="Save Changes" Width="150" Style="{StaticResource buttonStyle}" FontWeight="DemiBold" Click='OnButtonClickSaveChanges'/>
    </Grid>
</Window>
