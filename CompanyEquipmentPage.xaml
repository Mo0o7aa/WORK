<Page x:Class="ntra_missions.CompanyEquipmentPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:ntra_missions"
      mc:Ignorable="d"
      Title="Company Equipment">

    <Grid Background="{StaticResource AppBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <local:PageHeader x:Name="pageHeader" Title="NTRA Equipment" CurrentTag="CompanyEquipment"/>

        <Grid Grid.Row="1" Margin="24,16">
            <Grid.RowDefinitions>
                <RowDefinition Height="44"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <RadioButton Name="handheldToggle" Style="{StaticResource TogglePill}" Content="Handheld" IsChecked="True"
                             Checked="OnCheckedHandheld"/>
                <RadioButton Name="vehiclesToggle" Style="{StaticResource TogglePill}" Content="Vehicles"
                             Checked="OnCheckedVehicles"/>
            </StackPanel>

            <Border Grid.Row="1" Style="{StaticResource CardBorder}" VerticalAlignment="Stretch">
                <Grid>
                    <ListBox Name="handheldList" Style="{StaticResource CardList}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="0,0,0,1" Padding="8,10">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Number}" Style="{StaticResource ValueTextBrand}" Width="40"/>
                                        <TextBlock Text="{Binding Text}" Style="{StaticResource ValueText}"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <ListBox Name="vehiclesList" Style="{StaticResource CardList}" Visibility="Collapsed">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="0,0,0,1" Padding="8,10">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Number}" Style="{StaticResource ValueTextBrand}" Width="40"/>
                                        <TextBlock Text="{Binding Text}" Style="{StaticResource ValueText}"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="2" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource CardBorderBrush}"
                BorderThickness="0,1,0,0" Padding="16,10">
            <Button Style="{StaticResource PrimaryButton}" Content="Add" Click="OnBtnClickAdd" HorizontalAlignment="Center"/>
        </Border>
    </Grid>
</Page>
