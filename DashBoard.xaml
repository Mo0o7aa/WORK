<Page x:Class="ntra_missions.DashBoard"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:ntra_missions"
      xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
      mc:Ignorable="d"
      Title="DashBoard">

    <Grid Background="{StaticResource AppBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <local:PageHeader x:Name="pageHeader" Title="Dashboard" CurrentTag="DashBoard"/>

        <!-- Filter bar -->
        <Border Grid.Row="1" Style="{StaticResource CardBorder}" Margin="24,16,24,0" Padding="20,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <StackPanel Margin="0,0,28,0">
                    <TextBlock Text="Category" Style="{StaticResource FieldLabelText}" Margin="0,0,0,4"/>
                    <ComboBox Name="categoryCombo" Style="{StaticResource comboBoxStyle}" SelectionChanged="OnFilterChanged"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Margin="0,0,28,0">
                    <TextBlock Text="Start Date" Style="{StaticResource FieldLabelText}" Margin="0,0,0,4"/>
                    <DatePicker Name="startDatePicker" Style="{StaticResource datePickerStyle}" SelectedDateChanged="OnFilterChanged"/>
                </StackPanel>
                <StackPanel Grid.Column="2">
                    <TextBlock Text="End Date" Style="{StaticResource FieldLabelText}" Margin="0,0,0,4"/>
                    <DatePicker Name="endDatePicker" Style="{StaticResource datePickerStyle}" SelectedDateChanged="OnFilterChanged"/>
                </StackPanel>
                <TextBlock Grid.Column="3" Name="filterHintText" Style="{StaticResource FieldLabelText}"
                           VerticalAlignment="Bottom" HorizontalAlignment="Right" TextWrapping="Wrap"/>
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Margin="24,16,24,16">
            <StackPanel>
                <WrapPanel Name="kpiPanel" Margin="0,0,0,4"/>
                <WrapPanel Name="chartsPanel"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
