<UserControl x:Class="ntra_missions.PageHeader"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <StackPanel>

        <!-- Brand bar -->
        <Border Background="{StaticResource BrandBrush}">
            <Grid Height="72" Margin="16,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Image Source="Photos\NTRA_white1.png" Height="64" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Spectrum Monitoring CRM" Foreground="White" FontFamily="{StaticResource AppFont}"
                                   FontSize="18" FontWeight="Bold"/>
                        <TextBlock x:Name="pageTitleText" Foreground="#B9C2F0" FontFamily="{StaticResource AppFont}"
                                   FontSize="13" FontWeight="SemiBold"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Background="#26FFFFFF" CornerRadius="8" Padding="12,7" Margin="4,0" VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE77B;" FontFamily="Segoe MDL2 Assets" Foreground="White"
                                       FontSize="14" VerticalAlignment="Center" Margin="0,0,7,0"/>
                            <TextBlock x:Name="userNameText" Foreground="White" FontFamily="{StaticResource AppFont}"
                                       FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    <Button Style="{StaticResource HeaderChromeButton}" Content="Change Password" Click="OnClickChangePassword"/>
                    <Button Style="{StaticResource HeaderChromeButton}" Content="Sign out" Click="OnClickSignOut"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Navigation bar -->
        <Border Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="0,0,0,1">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                <StackPanel x:Name="navPanel" Orientation="Horizontal" Margin="12,7"/>
            </ScrollViewer>
        </Border>

    </StackPanel>
</UserControl>
