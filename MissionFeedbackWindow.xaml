<Window x:Class="ntra_missions.MissionFeedbackWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ntra_missions"
        mc:Ignorable="d"
        Title="Mission Feedback" Height="640" Width="1000" ResizeMode="CanResize"
        WindowStartupLocation="CenterScreen" Background="#F2F4F9">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="72"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource BrandBrush}">
            <Grid Margin="16,0">
                <Image Source="Photos\NTRA_white1.png" VerticalAlignment="Center" Height="52" HorizontalAlignment="Left"/>
                <Label Name="headerLabel" Content="Mission Feedback" Style="{StaticResource headerLabelStyle}"
                       FontSize="24" HorizontalAlignment="Center" Margin="0"/>
            </Grid>
        </Border>

        <TextBlock Grid.Row="1" Margin="28,18,28,0" TextWrapping="Wrap" Style="{StaticResource FieldLabelText}"
                   Text="Tick every sector that was enhanced (cleared) during this mission, then Save. Unticked sectors are recorded as still interfered."/>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel Name="sitesStackPanel" Margin="28,16"/>
        </ScrollViewer>

        <Border Grid.Row="3" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource CardBorderBrush}"
                BorderThickness="0,1,0,0" Padding="16,12">
            <Button Name="finishButton" Content="Save Changes" Style="{StaticResource PrimaryButton}"
                    HorizontalAlignment="Center" IsDefault="True" Click="OnButtonClickSaveChanges"/>
        </Border>
    </Grid>
</Window>
