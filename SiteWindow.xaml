<Window x:Class="ntra_missions.SiteWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ntra_missions"
        mc:Ignorable="d"
        Title="Site" Height="560" Width="820" WindowStartupLocation="CenterScreen" Background="#F2F4F9">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="72"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource BrandBrush}">
            <Grid Margin="16,0">
                <Image Source="Photos\NTRA_white1.png" VerticalAlignment="Center" Height="52" HorizontalAlignment="Left"/>
                <Label Name="headerLabel" Content="Site" Style="{StaticResource headerLabelStyle}"
                       FontSize="24" HorizontalAlignment="Center" Margin="0"/>
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="1" Margin="32,24" VerticalScrollBarVisibility="Auto">
            <Border Style="{StaticResource CardBorder}" Padding="28">
                <Grid Name="mainGrid">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="48"/>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Company Name" Style="{StaticResource FieldLabelText}" Margin="0,0,0,18"/>
                    <ComboBox Grid.Column="1" Name="companyNameComboBox" Style="{StaticResource comboBoxStyle}"
                              Width="Auto" HorizontalAlignment="Stretch" Margin="0,0,0,18" SelectionChanged="OnSelChangedCompanyNameCombo"/>
                    <TextBlock Grid.Column="3" Text="Site Number" Style="{StaticResource FieldLabelText}" Margin="0,0,0,18"/>
                    <TextBox Grid.Column="4" Name="siteNumberTextBox" Style="{StaticResource ModernTextBoxBase}" Margin="0,0,0,18"/>

                    <TextBlock Grid.Row="1" Text="City" Style="{StaticResource FieldLabelText}" Margin="0,0,0,18"/>
                    <ComboBox Grid.Row="1" Grid.Column="1" Name="cityComboBox" Style="{StaticResource comboBoxStyle}"
                              Width="Auto" HorizontalAlignment="Stretch" Margin="0,0,0,18" SelectionChanged="OnSelChangedCityCombo"/>
                    <TextBlock Grid.Row="1" Grid.Column="3" Text="Region" Style="{StaticResource FieldLabelText}" Margin="0,0,0,18"/>
                    <TextBox Grid.Row="1" Grid.Column="4" Name="regionTextBox" Style="{StaticResource ModernTextBoxBase}" Margin="0,0,0,18"/>

                    <TextBlock Grid.Row="2" Text="Latitude" Style="{StaticResource FieldLabelText}"/>
                    <TextBox Grid.Row="2" Grid.Column="1" Name="latTextBox" Style="{StaticResource ModernTextBoxBase}"
                             PreviewTextInput="NumberValidationTextBox"/>
                    <TextBlock Grid.Row="2" Grid.Column="3" Text="Longitude" Style="{StaticResource FieldLabelText}"/>
                    <TextBox Grid.Row="2" Grid.Column="4" Name="longTextBox" Style="{StaticResource ModernTextBoxBase}"
                             PreviewTextInput="NumberValidationTextBox"/>
                </Grid>
            </Border>
        </ScrollViewer>

        <Border Grid.Row="2" Name="buttonsGrid" Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource CardBorderBrush}" BorderThickness="0,1,0,0" Padding="16,12">
            <Button Name="finishButton" Content="Save Changes" Style="{StaticResource PrimaryButton}"
                    HorizontalAlignment="Center" IsDefault="True" Click="OnButtonClickSaveChanges"/>
        </Border>
    </Grid>
</Window>
